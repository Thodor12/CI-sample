name: Create Release Version

on:
    workflow_dispatch:
    schedule:
        - cron: 0 16 * * 0

jobs:
    release:
        uses: ./.github/workflows/reuse/create_release_version.yaml
        secrets: inherit
