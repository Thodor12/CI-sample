name: Create Release Version

on:
    workflow_dispatch:
    schedule:
        - cron: 0 16 * * 0

jobs:
    release:
        uses: ./.github/workflows/create_release_version_reuse.yaml
        secrets: inherit
